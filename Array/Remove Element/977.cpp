#include "common.h"

class Solution {
public:
    vector<int> sortedSquares(vector<int>& nums) {
        int left = 0,right = nums.size();
        for (; left < right; left++)
        {
            if (nums[left] < 0)
            {
                /* code */
            }
            
        }
         
    }
};

// 测试代码（可选）
int main() {
    Solution solution;
    
    // 添加测试用例
    
    return 0;
}
